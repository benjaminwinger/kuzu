add_subdirectory(shell)
if(${BUILD_NODEJS})
    add_subdirectory(nodejs_api)
endif()
add_subdirectory(python_api)
add_subdirectory(rust_api/shim)
if(${BUILD_BENCHMARK})
    add_subdirectory(benchmark)
endif()
